# Project Work State

This file is the durable handoff for work that spans Codex tasks. Keep entries concise; product decisions and implementation traceability belong in `DESIGN.md` and `implementation-map.yaml`.

See `DEVELOPMENT.md` for the new-task startup, GitHub, Supabase, verification, and synchronization workflow.

Synchronization note: **start the next working session from local `main`**. Local `main` intentionally contains the post-merge PR #34 handoff commit beyond `origin/main`; do not reset local `main` to `origin/main` and do not push the handoff directly to `origin/main`. Create the next feature branch from local `main` so the handoff is carried through the next pull request. The user-authored `UI-DESIGN-REVIEW-2026-09-07-v2.md` remains intentionally untracked and was not included in PR #34; preserve it unless Peter explicitly directs otherwise.

## Active

- No feature set is active. Select the next scope from the documented follow-up work below after reading the project documentation and current traceability entries.

## Recently completed

- First-round verified design gaps merged through GitHub PR #34 at merge commit `0640a02`. GitHub Verify run 88 and Vercel passed. The release adds accessible workload semantics, a direct-relationship activity timeline, simplified saturated overview/project/activity tiles, aligned interaction states, responsive regression coverage, and consistently `DEV`-named development fixtures including team members. Implementation-map validation, typecheck, lint, all 78 unit tests, the production build, and all 15 Playwright checks across 390×844 mobile, 1440×900 desktop, and the intermediate relationship-timeline viewport passed. The dedicated development fixture was reset successfully; no schema migration was included.
- Build/release guidance and audited traceability-gap corrections merged through GitHub PR #33 at merge commit `9669d2a`. GitHub Verify run 85 and Vercel passed. `DEVELOPMENT.md` now defines the gated delivery path, risk-based suite selection, and required user notification for disproportionate or insufficient coverage. The release adds read-only archived project/activity detail routing, activity archive impact confirmation, activity timing and dependent visibility, bottom-of-detail links, template lead-in summaries, and top-level dialog focus containment/restoration; it also reconciles stale DEP-02, DEP-03, DEP-11, template-suite, and superseded dependency-editor traceability claims. All 78 unit tests, the production build, and 14 read-only Playwright checks across 390×844 mobile and 1440×900 desktop passed. DEP-04 full path explanation, DEP-05 relationship timeline lanes/filters, DEP-08 message detail, QA-05 mutation/CI coverage, and automated accessibility auditing remain follow-up work. Valid direct-date confirmation under DEP-11 and workflow/priority sort ordering require clarification before behavioral changes. Local `main` was synchronized with `origin/main`, and the merged feature branch was removed locally and remotely before this handoff commit.
- DEP-11 dated activity schedule rules merged through GitHub PR #32 at merge commit `09af4ab`. GitHub Verify run 83 and the Vercel preview and production deployments passed. The release adds the preservation-first, current-activity-only planner, unified Schedule rule modal, unsaved-date evaluation, exact adjustment confirmation, incoming fixed-boundary context, dependency offsets, portfolio-wide filtered activity picker, inline referenced-activity creation, and final-save revalidation with QA-03 coverage. Template resolution now derives arbitrary-depth dependent branches from a resolved project-end-anchored graph and preserves offsets during materialization. All 78 repository tests and 107 disposable-database assertions pass; local, development, and production schema lint are clean. Migrations `202609070001` through `202609070004` are applied to development and production with no pending migrations. Authorized desktop, 390×844, and Peter-led transitive-template validation passed. Local Supabase configuration was restored to development, local `main` was synchronized, and the merged feature branch was removed locally and remotely. Multi-activity optimization for dated activities remains deferred.
- Data API privilege consistency and hardening merged through GitHub PR #31 at merge commit `fef19dc`. GitHub Verify run 81 passed both repository and disposable-Supabase database jobs, and Vercel passed on final head `477b151`. Migrations `202609060004` and `202609060005` are applied to development and production; the environments now match exactly across Data API grants, RLS policies, schema access, and default privileges. Anonymous production table access returns HTTP 401, authenticated production reads remain functional, and all 94 database assertions pass. Local configuration and CLI linking were restored to development, the LAN-enabled local server returns HTTP 200, authenticated 390×844 validation loads and navigates the reserved fixtures without overflow or browser/origin errors, and Peter confirmed the application works. Local `main` is synchronized with `origin/main`, and the merged feature branch was removed locally and remotely before this handoff commit.
- Dedicated development Supabase setup merged through GitHub PR #30 at merge commit `d6bb322`. GitHub Verify run 78 passed both repository and disposable-Supabase database jobs, Vercel passed on final head `e7b264b`, local and remote feature branches were removed, and local `main` was synchronized with `origin/main`. All 21 then-current migrations are applied to the separate development project; local configuration defaults to development with guarded target matching and explicit write authorization, and the repeatable reserved fixture graph contains 3 projects, 5 activities, and 3 team members without copied production data.
- QA-04 activity work-management database coverage and required CI enforcement merged through GitHub PR #29 at merge commit `fa77ec5`. The new 37-assertion transactional suite covers shared authenticated reads, anonymous isolation, authoritative aggregate activity saves, blocked direct writes, dependency-cycle rollback, dependency chronology, project-relative timing and exception boundaries, atomic project-start adjustment, and activity/project archive retention. GitHub Verify run 75 passed both the `verify` and disposable-Supabase `database` jobs, Vercel passed on final head `f921ebd`, and the complete local database suite passed 82 assertions. Local `main` was synchronized to `origin/main`, and the merged feature branch was removed locally and remotely before this handoff commit.
- Template multiple constraints merged through GitHub PR #28 at merge commit `9705f11`. GitHub Verify run 69 and Vercel passed on final head `ed11e6f`; the complete empty-state migration replay, 64 unit checks, 45 database assertions, traceability verification, production build, linked migration-history check, and no-pending-migration dry run pass. Migrations `202609060002` and `202609060003` are applied to the linked backend. The release adds positive inclusive durations, staged multiple schedule rules, latest-valid graph resolution including derived prerequisites, explicit and inverse relationship summaries, read-only linked incoming rules, mobile table refinements, Chrome iOS hydration compatibility, and plain-HTTP LAN-safe temporary identifiers. Local `main` was synchronized to `origin/main`, and the merged feature branch was removed locally and remotely before this handoff commit.
- Project templates and project-from-template creation merged through GitHub PR #27 at merge commit `ef49009`. GitHub Verify run 65 and Vercel passed on final head `0196825`; `pnpm verify` passed with 64 tests, the production build passed, linked schema lint was clean, and `pnpm db:test` passed 31 assertions including the 12-case template suite. Authorized Docker-backed desktop and 390×844 mobile journeys plus physical-iPhone validation covered template authoring, inline dependency creation, deterministic preview dates, atomic Draft/Not Started materialization, responsive containment, navigation, and linked-backend connectivity. Migration `202609060001_project_templates.sql` is applied to the linked Supabase backend. Local and remote feature branches were removed, and local `main` was synchronized to `origin/main` before this handoff commit.
- The phased responsive-web UX follow-up merged through GitHub PR #26 at merge commit `063e581`. GitHub Verify run 61 and Vercel passed on final head `023cb27`; `pnpm verify` passed with 58 tests, the production build passed, and Chromium smoke coverage passed 8/8 mobile checks at 390×844 and 4/4 desktop checks at 1440×900. The release replaces the unstable fixed mobile bottom navigation with a document-flow ginkgo header and complete drawer; standardizes responsive controls, filters, editors, typography, and containment; fixes mobile project schedules with a daily scale and Monday labels; and adds shared project tiles comparing elapsed time with activity completion. Commit `9cd0e87` remains the clean reference for the superseded Chrome workaround. The feature branch was removed locally and remotely, and local `main` was synchronized to `origin/main` before this handoff commit.
- The Instinct/mobile UX review and QA-05 browser-smoke release gate merged through GitHub PR #25 at merge commit `ca94e6c`. Repository Verify and Vercel passed on the final PR head; `pnpm verify` passed with 51 tests, the production build passed, all seven persistent Playwright checks passed across 390×844 mobile and 1440×900 desktop, and the feature branch was removed locally and remotely. The release implemented responsive navigation, filters, schedules, mobile cards, accessible form validation and activity disclosures, corrected project-team and Save controls, unified activity CTAs and empty states, stronger table/card legibility and contrast, and the Today treatment. Rejected or deferred review items remain documented and out of scope: top-drawer navigation, a mandatory 768px breakpoint, renaming All Events, a blanket timeline legend, wholesale copy/restyling, and unnecessary archive restyling.
- PRJ-06 Slice 3 and its release gate merged through GitHub PR #24 at merge commit `4c38fe3`. Repository and Vercel checks passed on the final PR head. `pnpm verify` passed with 48 tests and the production build passed; authorized live testing covered desktop and 390×844 mobile layout, keyboard cancellation/confirmation, atomic movement, conflict recovery, and stale-preview refresh with all test data restored. Local `main` was synchronized and the merged feature branch was removed locally and remotely.
- PRJ-06 Slice 2.1 merged through GitHub PR #22 at merge commit `ae5b130`. Vercel passed on the final PR head, and local `main` was fast-forwarded to `origin/main`; the merged feature branch was removed locally and remotely.
- Slice 2.1 verification handoff: migration `202609050002` is applied to the linked Supabase project; linked lint, generated types, `pnpm verify`, and production build pass. The complete migration history replays from empty local state; all 19 pgTAP assertions and the explicit-barrier `PRJ06-CON-01`–`PRJ06-CON-13` matrix pass. The matrix found the authenticated archive commit defect corrected by `202609050002`; rerun the matrix after any concurrency-path change. Docker Desktop folder sharing must remain enabled for this repository.

## Next

- Extend scheduling unit coverage as later project-rescheduling slices and dependency behaviors are implemented (`QA-03`).
- Extend QA-05 beyond the responsive smoke slice with disposable-fixture authentication, overview, CRUD, dependency, timeline, and administration journeys, then make it a required CI job.
- Establish a protected Vercel preview pathway for reviewing committed work from Peter's mobile devices away from the local network. Verify and document that the preview application targets the dedicated development Supabase backend, confirm authentication and redirect behavior at the preview origin, choose an appropriately restricted access policy, and record which read-only and data-changing validation is permitted. Keep application deployment separate from database migration, require the reserved development fixture and verified cleanup for mutations, and document how to obtain the current branch or commit preview URL without exposing credentials.

## Distant future

- Consider a de novo native mobile client built with Expo and React Native after the mobile and desktop web experiences are robust. Keep the existing Next.js application unchanged as the web client; share the Supabase backend, authentication, row-level security, generated data types, and suitable pure scheduling logic while designing native navigation and screens independently. Begin only with a small read-only iPhone prototype covering sign-in, overview, projects, project detail, activity sequence, and activity detail, then evaluate the native experience before authorizing editing, notifications, TestFlight, or App Store release work.

## Working conventions

- Start from a requirement ID in `implementation-map.yaml`.
- Keep each commit focused on one concern and include its map/design/test updates.
- Run `pnpm verify` before committing.
- Never record credentials, access tokens, database passwords, or user data here.
